# Deny script execution inside uploaded files (see root uploads/.htaccess).
<FilesMatch \.(?i:php|php3|php4|php5|php7|phar|phtml|pl|py|cgi|sh)$>
    Require all denied
</FilesMatch>
Options -ExecCGI
RemoveHandler .php .phtml .phar

<IfModule mod_php.c>
    php_flag engine off
</IfModule>
